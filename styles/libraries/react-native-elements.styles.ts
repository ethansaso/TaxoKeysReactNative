import { StyleSheet } from 'react-native';

export const rnElementsStyles = StyleSheet.create({
    button: {
        borderRadius: 12,
    },
    buttonWarning: {
        backgroundColor: '#fbc658',
    },
});